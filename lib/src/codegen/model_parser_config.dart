class ModelParserConfig {
  final String outputPath;
  final String outputClassName;

  final bool requireToJson;

  ModelParserConfig({
    required this.outputPath,
    required this.outputClassName,
    required this.requireToJson,
  });
}
